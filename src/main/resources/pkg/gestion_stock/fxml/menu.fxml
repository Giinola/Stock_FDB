<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<BorderPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="pkg.gestion_stock.controllers.MenuController"
            prefWidth="1200" prefHeight="700">

    <!-- SIDEBAR GAUCHE -->
    <left>
        <VBox styleClass="sidebar" prefWidth="250"
              style="-fx-background-color: #2c3e50; -fx-padding: 20;">

            <!-- Logo / Titre -->
            <VBox alignment="CENTER" spacing="5" style="-fx-padding: 0 0 30 0;">
                <Text text="📦 STOCK MANAGER"
                      style="-fx-fill: white; -fx-font-size: 20; -fx-font-weight: bold;"/>
                <Text text="Gestion de Stock"
                      style="-fx-fill: #95a5a6; -fx-font-size: 12;"/>
            </VBox>

            <Separator style="-fx-background-color: #34495e;"/>

            <!-- Menu Items -->
            <VBox spacing="10" VBox.vgrow="ALWAYS" style="-fx-padding: 20 0 0 0;">

                <Button fx:id="btnDashboard" text="🏠  Dashboard"
                        onAction="#showDashboard"
                        styleClass="menu-button" maxWidth="Infinity"
                        style="-fx-background-color: #3498db; -fx-text-fill: white;
                               -fx-font-size: 14; -fx-padding: 12; -fx-alignment: CENTER_LEFT;
                               -fx-background-radius: 5;"/>

                <Button fx:id="btnProduits" text="📋  Liste des Produits"
                        onAction="#showProduits"
                        styleClass="menu-button" maxWidth="Infinity"
                        style="-fx-background-color: transparent; -fx-text-fill: #ecf0f1;
                               -fx-font-size: 14; -fx-padding: 12; -fx-alignment: CENTER_LEFT;
                               -fx-background-radius: 5;"/>

                <Button fx:id="btnEntree" text="⬇️  Entrée en Stock"
                        onAction="#showEntreeStock"
                        styleClass="menu-button" maxWidth="Infinity"
                        style="-fx-background-color: transparent; -fx-text-fill: #ecf0f1;
                               -fx-font-size: 14; -fx-padding: 12; -fx-alignment: CENTER_LEFT;
                               -fx-background-radius: 5;"/>

                <Button fx:id="btnSortie" text="⬆️  Sortie en Stock"
                        onAction="#showSortieStock"
                        styleClass="menu-button" maxWidth="Infinity"
                        style="-fx-background-color: transparent; -fx-text-fill: #ecf0f1;
                               -fx-font-size: 14; -fx-padding: 12; -fx-alignment: CENTER_LEFT;
                               -fx-background-radius: 5;"/>

                <Button fx:id="btnInventaire" text="📊  Inventaire"
                        onAction="#showInventaire"
                        styleClass="menu-button" maxWidth="Infinity"
                        style="-fx-background-color: transparent; -fx-text-fill: #ecf0f1;
                               -fx-font-size: 14; -fx-padding: 12; -fx-alignment: CENTER_LEFT;
                               -fx-background-radius: 5;"/>

                <Region VBox.vgrow="ALWAYS"/>

                <Separator style="-fx-background-color: #34495e;"/>

                <Button text="⚙️  Paramètres"
                        styleClass="menu-button" maxWidth="Infinity"
                        style="-fx-background-color: transparent; -fx-text-fill: #95a5a6;
                               -fx-font-size: 12; -fx-padding: 10; -fx-alignment: CENTER_LEFT;"/>
            </VBox>
        </VBox>
    </left>

    <!-- CONTENU PRINCIPAL -->
    <center>
        <StackPane fx:id="contentArea" style="-fx-background-color: #ecf0f1;">
            <!-- Les vues seront chargées ici dynamiquement -->
        </StackPane>
    </center>

</BorderPane>